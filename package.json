{
  "name": "node-cgns",
  "version": "1.0.0",
  "description": "An CGNS bindings for Node.js",
  "main": "index.js",
  "author": "Colin Han <biz@colinhan.info>",
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.3.0",
    "node-gyp": "^3.6.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^3.5.3"
  },
  "scripts": {
    "build": "./node_modules/.bin/node-gyp configure && ./node_modules/.bin/node-gyp build --debug",
    "build:prod": "./node_modules/.bin/node-gyp configure && ./node_modules/.bin/node-gyp build",
    "test": "./node_modules/.bin/mocha"
  }
}
